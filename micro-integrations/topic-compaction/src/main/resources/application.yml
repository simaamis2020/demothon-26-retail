# =============================================================================
# Internal Micro-Integration Configuration
# This file is packed inside the MI distributable.
# Operators MUST NOT modify this file - external config goes in
# /app/external/spring/config/application.yml (mounted at runtime).
# =============================================================================

spring:
  application:
    name: topic-compaction-mi
  cloud:
    config:
      import-check:
        enabled: false

    stream:
      # [INTERNAL] Pre-create input/output bindings.
      # We reserve 4 each for V1: data ingress, command, lookup-rr, audit-out, replay-out.
      # Extra slack lets operators add custom workflows without rebuilding the image.
      input-bindings: "input-0;input-1;input-2;input-3;input-4;input-5;input-6;input-7"
      output-bindings: "output-0;output-1;output-2;output-3;output-4;output-5;output-6;output-7"

      default-binder: undefined

      default:
        consumer:
          # [INTERNAL] Auto-startup must always be disabled - MI Framework controls lifecycle
          auto-startup: false
          retryable-exceptions.java.lang.InterruptedException: false
          # [INTERNAL] Skip Spring's payload conversion - we want raw bytes
          use-native-decoding: true
          # [INTERNAL] Prevents mutated nested objects from persisting between local retries
          max-attempts: 1
        producer:
          auto-startup: false
          error-channel-enabled: true
          use-native-encoding: true

      # Dummy consumer groups required by the framework
      bindings:
        input-0: {group: group-0}
        input-1: {group: group-1}
        input-2: {group: group-2}
        input-3: {group: group-3}
        input-4: {group: group-4}
        input-5: {group: group-5}
        input-6: {group: group-6}
        input-7: {group: group-7}

      solace:
        default:
          consumer:
            add-destination-as-subscription-to-queue: false
            batch-wait-strategy: immediate
            provision-durable-queue: false
            provision-error-queue: false
            queue-name-expression: "destination"
          producer:
            header-exclusions:
              - id
            provision-durable-queue: false

solace:
  java:
    api-properties:
      # JCSMP publish-window. Smaller window than the default (255)
      # gives the binder backpressure earlier on slow brokers, so a
      # publish burst cannot saturate the in-flight buffer and starve
      # consumer-acks. 50 is enough headroom for a single workflow's
      # bursty audit traffic without inflating tail-latency.
      pub_ack_window_size: 50
      # JCSMP publish-ack timeout in milliseconds. The default (2000)
      # is hostile under any non-trivial broker load: a 5-message
      # burst routinely sees the trailing publishes time out before
      # the broker acks, which keeps the inbound consumer-ack pinned
      # in the pub-ack-pending state and surfaces as txUnackedMsgCount
      # > 0 even though the KV write already succeeded. 30s is the
      # operating-system-level "this is genuinely broken" threshold;
      # under that, transient slowness is absorbed cleanly.
      pub_ack_time: 30000
      # NOTE: KeepAlive tuning lives on JCSMPChannelProperties (a
      # sub-object), which is not reachable via the api-properties
      # text-marshaling path - JCSMPPropertiesTextMarshaling logs
      # "Skipping property key ... unknown" for those keys. A
      # BeanPostProcessor on SpringJCSMPFactory is the right hook;
      # deferred to V1.1 alongside the direct-audit publisher.
  connector:
    management:
      session:
        reconnect-retries: -1

    # [INTERNAL] Always-Enabled Message Transforms feature - mandatory per MI SDK 3.0+
    default:
      workflow:
        transform:
          enabled: true
        # MI Framework's AsyncOutputSendingMessageHandler waits this
        # long for a publish-ack callback before resolving the
        # consumer-ack with failure. Default is 10 minutes - hostile
        # in production: if a single audit publish is silently
        # discarded by the broker (no ack, no nack), the inbound
        # message stays pinned in txUnackedMsgCount for 10 minutes
        # before the framework gives up and lets the consumer-ack
        # flush via the error-channel. 30 s aligns with our JCSMP
        # pub_ack_time and keeps the worst-case stuck-spool window
        # to 30 s instead of 600 s.
        acknowledgment:
          publish-timeout: 30000

    # [INTERNAL] Pre-wire workflows. Operators enable individual workflows
    # via external config (workflows.<idx>.enabled = true).
    workflows:
      0: {input-bindings: [input-0], output-bindings: [output-0]}
      1: {input-bindings: [input-1], output-bindings: [output-1]}
      2: {input-bindings: [input-2], output-bindings: [output-2]}
      3: {input-bindings: [input-3], output-bindings: [output-3]}
      4: {input-bindings: [input-4], output-bindings: [output-4]}
      5: {input-bindings: [input-5], output-bindings: [output-5]}
      6: {input-bindings: [input-6], output-bindings: [output-6]}
      7: {input-bindings: [input-7], output-bindings: [output-7]}

# =============================================================================
# Topic Compaction MI specific settings (defaults; overridable externally)
# =============================================================================
topic-compaction:
  kvstore:
    backend: rocksdb           # rocksdb | caffeine
    rocksdb:
      path: ./data/rocksdb
      max-open-files: 1000
    caffeine:
      maximum-size: 1000000
  compaction:
    audit-suffix: /compacted-ack
    loop-protection-header: x-compacted-replay
    ordering:
      header: ""               # empty = always-last-wins; e.g. "senderTimestamp"
  replay:
    target-suffix: /compacted

# =============================================================================
# Spring Boot Actuator + logging
# =============================================================================
logging:
  level:
    root: warn
    com.solace.labs.mi.topiccompaction: info

management:
  # Disable all metric registry exporters by default so we do not
  # accidentally light up exporters we never configured. Prometheus
  # is re-enabled explicitly below.
  defaults:
    metrics:
      export:
        enabled: false
  prometheus:
    metrics:
      export:
        enabled: true
  # OTLP tracing pipeline. Endpoint defaults to localhost so a
  # missing collector does not crash the app; cluster overrides set
  # OTEL_EXPORTER_OTLP_ENDPOINT to the in-cluster Tempo service.
  tracing:
    sampling:
      probability: 1.0
    enabled: true
  otlp:
    tracing:
      endpoint: ${OTEL_EXPORTER_OTLP_ENDPOINT:http://localhost:4317}
      transport: grpc
  endpoints:
    web:
      exposure:
        include: "health,metrics,prometheus,loggers,logfile,channels,env,workflows,leaderelection,bindings,info"
  endpoint:
    env:
      show-values: WHEN_AUTHORIZED
    prometheus:
      access: read_only
    health:
      probes:
        # Expose dedicated /health/liveness and /health/readiness for
        # Kubernetes probes. The liveness group only checks the JVM,
        # readiness includes the Solace binder so traffic is held off
        # until bindings are UP.
        enabled: true
      group:
        liveness:
          include: livenessState
        readiness:
          include: readinessState,binders
      status:
        order:
          - DOWN
          - OUT_OF_SERVICE
          - RECONNECTING
          - UP
          - UNKNOWN
  metrics:
    # Common tags are attached programmatically in MetricsConfig so the
    # filter sees the resolved bean values rather than literal
    # placeholders. Distribution stats here apply globally.
    distribution:
      percentiles-histogram:
        http.server.requests: true
      percentiles:
        http.server.requests: 0.5,0.95,0.99

server:
  port: 8090
  # Graceful shutdown: stop accepting new HTTP connections
  # immediately on SIGTERM, then drain in-flight requests for up to
  # the configured timeout-per-shutdown-phase below. Combined with
  # the @PreDestroy hooks on KvStore (RocksDB WAL sync + close),
  # this gives an end-to-end clean shutdown contract.
  shutdown: graceful

spring.lifecycle.timeout-per-shutdown-phase: 25s
